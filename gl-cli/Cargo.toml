[package]
name = "gl-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
gl-core = { path = "../gl-core" }  # Import core logic
serde = { version = "1.0", features = ["derive"] }
csv = "1.0"